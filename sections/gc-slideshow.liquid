{%- comment -%}
  gc-slideshow ── slideshow.liquid 的副本（2026-09-01，遵守「不改主題原檔」規矩）
  差異只有三處：
  1. 每格多一個「說明文字」欄位（text），渲染在大標下方——原版只有小標＋大標＋按鈕。
  2. 手機（<=740px）文字疊層固定在圖片上方——原版置中會壓到置中構圖的產品主體。
  3. 大標字級放大（clamp 40~84px），對齊 Insta360 式的層級比例。
  還原方式：index.json 把 section type 從 gc-slideshow 改回 slideshow、刪掉 text 欄位即可。
{%- endcomment -%}
<style>
  #shopify-section-{{ section.id }} {
    --progress-bar-color: {{ section.blocks.first.settings.text_color.red }}, {{ section.blocks.first.settings.text_color.green }}, {{ section.blocks.first.settings.text_color.blue }};
    --section-autoplay-duration: {% if section.settings.autoplay %}{{ section.settings.cycle_speed }}s{% else %}0s{% endif %};
    --section-animation-play-state: paused;
    background-color: {{ section.settings.background }}; /* Allows to set a placeholder color while loading */
  }

  {%- for block in section.blocks -%}
    {%- assign text_position = block.settings.text_position | split: '_' | first -%}

    {%- case text_position -%}
      {%- when 'top' -%}
        {%- assign section_items_alignment = 'flex-start' -%}
      {%- when 'middle' -%}
        {%- assign section_items_alignment = 'center' -%}
      {%- when 'bottom' -%}
        {%- assign section_items_alignment = 'flex-end' -%}
    {%- endcase -%}

    #block-{{ section.id }}-{{ block.id }} {
      --heading-color: {{ block.settings.text_color.red }}, {{ block.settings.text_color.green }}, {{ block.settings.text_color.blue }};
      --text-color: {{ block.settings.text_color.red }}, {{ block.settings.text_color.green }}, {{ block.settings.text_color.blue }};
      --primary-button-background: {{ block.settings.button_background.red }}, {{ block.settings.button_background.green }}, {{ block.settings.button_background.blue }};
      --primary-button-text-color: {{ block.settings.button_text_color.red }}, {{ block.settings.button_text_color.green }}, {{ block.settings.button_text_color.blue }};

      --section-blocks-alignment: {{ section_items_alignment }};
      --section-blocks-overlay-color: {{ block.settings.overlay_color.red }}, {{ block.settings.overlay_color.green }}, {{ block.settings.overlay_color.blue }};
      --section-blocks-overlay-opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }};
    }
  {%- endfor -%}

  .slideshow .gc-slideshow__tagline {
    margin: 20px 0 0;
    font-size: clamp(16px, 1.5vw, 26px);
    line-height: 1.6;
    letter-spacing: .01em;
    color: rgba(var(--text-color), .92);
  }
  /* !important：跟 gc-type.css 的全站字級同權重會輸，這裡是輪播專屬層級要贏 */
  .slideshow .content-box .heading--large { font-size: clamp(40px, 4.2vw, 84px) !important; line-height: 1.08 !important; }
  /* 尾字特效＝Air 商品頁 .wordmark em 的原配方，改字先去那邊對 */
  .slideshow .gc-slideshow__accent { font-style: normal; font-weight: 100; letter-spacing: -.022em;
    font-family: "SF Pro Display", "SF Pro Text", -apple-system, BlinkMacSystemFont, "Helvetica Neue", "PingFang TC", "Noto Sans TC", sans-serif;
    background: linear-gradient(180deg, #f5f5f7 30%, #2997ff);
    -webkit-background-clip: text; background-clip: text; color: transparent; }
  /* 文字盒放寬（僅桌機）：大標 TITANSHIELD Pro 在 63px 字級要 ~600px 才放得下一行；
     不能無條件套——46% 在手機會把文字盒縮成一條，手機維持主題原本的滿寬 */
  @media screen and (min-width: 741px) {
    #shopify-section-{{ section.id }} .content-box.content-box--medium { max-width: min(680px, 46%) !important; }
  }
  #shopify-section-{{ section.id }} .content-box .heading--small { text-transform: none; font-size: clamp(16px, 1.6vw, 26px) !important; letter-spacing: .02em; }
  /* 手機：文字一律疊在圖片上方——手機構圖產品多半置中，置中疊字會壓到主角 */
  @media screen and (max-width: 740px) {
    #shopify-section-{{ section.id }} .slideshow__slide-inner { align-items: flex-start; }
    /* 手機頁首透明疊在圖上，起算點要加頁首高度；原版 padding 權重高，要 !important 才壓得過 */
    #shopify-section-{{ section.id }} .slideshow__text-wrapper { padding-top: calc(var(--header-height, 60px) + 28px) !important; padding-bottom: 0 !important; }
    .slideshow .content-box .heading--large { font-size: 34px !important; }
    #shopify-section-{{ section.id }} .content-box .heading--small { font-size: 15px !important; letter-spacing: .05em; }
    .slideshow .gc-slideshow__tagline { margin-top: 12px; font-size: 16px; }
  }

  /* 把首圖整段往上塞到頁首底下，頁首才有東西可以透
     （手機 2026-09-01、桌機 2026-09-02 一起）。
     用負邊界而不是把頁首改成 fixed —— 改 fixed 會影響全站每一頁，
     負邊界只作用在這一段，拿掉這一行就完全還原。

     桌機不用補回文字的位置：圖跟字是**一起**往上移的，
     字本來就置中在圖上，兩者的相對關係沒變（量過：字距畫面頂端 213px，
     頁首只有 65px 高，不會壓到）。手機是另一回事，
     手機的字靠上對齊，所以上面那段有加 padding-top 補頁首高度。 */
  {%- if section.settings.tuck_under_header != false -%}
  #shopify-section-{{ section.id }} { margin-top: calc(-1 * var(--header-height, 65px)); }
  @media screen and (max-width: 740px) {
    #shopify-section-{{ section.id }} { margin-top: calc(-1 * var(--header-height, 52px)); }
  }
  {%- endif -%}

  {%- comment -%}
    ── 白底首圖版型（2026-09-13）───────────────────────────────────────────────
    深底橫幅的字是「疊在圖上」，白底不能這樣做：白字會不見，而圖上方的純白又太窄
    （桌機只有整段的 14%，扣掉透明頁首剩 31px）。所以白底那張改成「字在上、產品貼底」，
    白色靠整段的底色跟圖自己的白接起來，看不出接縫。

    高度為什麼是「整段」的設定而不是「單張」的：輪播的作用中那張是 position:relative，
    其餘是 absolute 疊在它上面——**整段的高度等於作用中那張的高度**。只調一張的話，
    輪播每換一次頁面就會上下跳一次。所以高度一改就是四張一起改，
    代價是另外三張（cover 裁切）左右會被多裁一些。這是取捨，不是 bug。

    文字區用「佔整段高度的百分比」定位，不用 px：整段高度是 vw（跟著螢幕寬變），
    白區的位置也就跟著等比移動，一個數字在所有寬度都成立。
  {%- endcomment -%}

  {%- comment -%}
    高度的算法（2026-09-13 第二版，實測改的）

    第一版把整段高度寫死成「圖片的比例」，在 1440 剛好，但**換個寬度就爆**：
    白區是跟著寬度等比縮的，文字卻不是（按鈕固定 52px、頁首固定 65px、
    行高有下限），所以螢幕一窄，白區縮得比文字快，文字就頂穿白區壓到頁首。
    741～1100 這段（27 吋縮一半、平板橫放）整個壞掉就是這樣來的。

    治法兩件事：
    1. 整段高度取「圖片自然高」與「文字真正需要的高」兩者的大者，
       所以窄螢幕會自己長高一點，文字永遠塞得下。
    2. 白底那張的圖改成 contain 貼底——整段比圖高的時候，上面自動多出白，
       因為底色跟圖的上緣都是白的，接縫看不見；整段比圖矮的時候圖等比縮、
       左右露出的也是白。**兩個方向都不會裁到產品。**

    文字區的高度＝整段高 −「圖裡產品那一段」的高。產品那段用 min() 同時
    表示成 vw 與 %，是為了涵蓋上面兩種情形（以寬度為準 / 以高度為準）。
  {%- endcomment -%}

  {%- assign d_img_vw = section.settings.custom_height_desktop_vw -%}
  {%- assign m_img_vw = section.settings.custom_height_mobile_vw -%}
  {%- for block in section.blocks -%}
    {%- if block.settings.light_layout -%}
      {%- assign d_pct = block.settings.light_white_pct | default: 43.7 -%}
      {%- assign m_pct = block.settings.light_white_pct_mobile | default: 43.07 -%}
      {%- assign d_coef = 100.0 | minus: d_pct | divided_by: 100.0 -%}
      {%- assign m_coef = 100.0 | minus: m_pct | divided_by: 100.0 -%}
      {%- assign d_prod_vw = d_img_vw | times: d_coef -%}
      {%- assign m_prod_vw = m_img_vw | times: m_coef -%}
      {%- assign d_prod_pct = d_coef | times: 100 -%}
      {%- assign m_prod_pct = m_coef | times: 100 -%}
      {%- comment -%}文字要用掉的高度：頁首＋三行字＋按鈕＋離產品的間距，抓一個涵蓋所有寬度的上限{%- endcomment -%}
      {%- assign d_budget = 'clamp(460px, 24vw, 535px)' -%}
      {%- assign m_budget = 350 -%}
      {%- comment -%}
        圖片一律「滿寬貼底」，不用 object-fit。
        contain 會在寬螢幕把圖縮小、左右露出兩條白帶——底色跟圖片下緣的陰影
        差幾個色階，那條邊在大螢幕上看得出來（2026-09-13 在 2000px 寬抓到）。
        cover 則反過來，在窄螢幕會裁掉產品左右。
        直接 width:100% + bottom:0 兩種都不會發生：整段比圖高就在上面多出白，
        比圖矮就從上面裁掉純白那段（裁掉也看不出來）。
        所以產品在畫面上的高度恆等於 prod_vw，白區＝整段高 − prod_vw。
        也因此不能給整段封高度上限——封頂會連放字的白一起吃掉。
      {%- endcomment -%}
      #block-{{ section.id }}-{{ block.id }} .slideshow__image {
        position: absolute !important;
        inset: auto 0 0 0 !important;
        width: 100% !important;
        height: auto !important;
        max-width: none !important;
      }
      {%- if d_img_vw > 0 -%}
        #shopify-section-{{ section.id }} .slideshow__slide,
        #shopify-section-{{ section.id }} .slideshow__slide-inner {
          min-height: calc({{ d_prod_vw }}vw + {{ d_budget }}) !important;
        }
      {%- endif -%}
      {%- if m_img_vw > 0 -%}
        @media screen and (max-width: 740px) {
          #shopify-section-{{ section.id }} .slideshow__slide,
          #shopify-section-{{ section.id }} .slideshow__slide-inner {
            min-height: calc({{ m_prod_vw }}vw + {{ m_budget }}px) !important;
          }
        }
      {%- endif -%}
      #block-{{ section.id }}-{{ block.id }} .slideshow__text-wrapper {
        height: calc(100% - {{ d_prod_vw }}vw) !important;
      }
      @media screen and (max-width: 740px) {
        #block-{{ section.id }}-{{ block.id }} .slideshow__text-wrapper {
          height: calc(100% - {{ m_prod_vw }}vw) !important;
        }
      }
    {%- endif -%}
  {%- endfor -%}

  .slideshow .gc-slideshow__tagline--sub {
    margin-top: 8px;
    font-size: clamp(15px, 1.25vw, 19px);
    color: rgba(var(--text-color), .68);
  }
  @keyframes gc-gold-sweep { 0%, 100% { background-position: 6% 0; } 50% { background-position: 94% 0; } }

  {%- for block in section.blocks -%}
    {%- if block.settings.light_layout -%}
      {%- assign light_bg = block.settings.light_bg | default: '#fefefe' -%}
      {%- comment -%}
        底色要跟圖片最上緣那一片白「同一個色」——量過原圖，最常見的就是 (254,254,254)。
        差幾個色階，交界處就會看得出一條邊。
      {%- endcomment -%}
      #block-{{ section.id }}-{{ block.id }},
      #block-{{ section.id }}-{{ block.id }} .slideshow__image-wrapper { background: {{ light_bg }}; }
      /* contain 貼底：整段比圖高就往上多出白，比圖矮就等比縮、左右露白——兩邊都不裁產品 */
      #block-{{ section.id }}-{{ block.id }} .slideshow__image {
        object-fit: contain !important;
        object-position: bottom center !important;
      }
      #block-{{ section.id }}-{{ block.id }} .container {
        position: absolute; inset: 0; max-width: none; padding: 0; z-index: 2;
      }
      /* 文字在白區裡上下置中，padding 當下限：按鈕離產品至少 48px（手機 36px），
         寬螢幕多出來的白上下均分，不會整團偏下。 */
      #block-{{ section.id }}-{{ block.id }} .slideshow__text-wrapper {
        position: absolute; left: 0; right: 0; top: 0;
        display: flex !important;
        flex-direction: column;
        justify-content: center !important;
        align-items: center;
        padding: var(--header-height, 65px) 24px 48px !important;
      }
      #block-{{ section.id }}-{{ block.id }} .heading--large {
        font-size: clamp(34px, 3.9vw, 70px) !important;
      }
      /* 主題把 .heading--large 明寫成 text-align:left（content-box 明明是 center）。
         窄螢幕文字盒剛好被字撐滿看不出來，寬螢幕文字盒一變寬，大標就整條貼左邊。 */
      #block-{{ section.id }}-{{ block.id }} .content-box > * { text-align: center !important; }
      /* 選擇器要壓過上面那條 `#shopify-section-… .content-box--medium{max-width:min(680px,46%)}`，
         兩條都有 !important，只能靠特異性贏——46% 在窄桌機會把大標擠成兩行，版面就垮了 */
      #shopify-section-{{ section.id }} #block-{{ section.id }}-{{ block.id }} .content-box.content-box--medium {
        max-width: min(900px, 92%) !important;
        width: 100% !important;
        text-align: center !important;
        align-items: center !important;
      }
      @media screen and (max-width: 740px) {
        #block-{{ section.id }}-{{ block.id }} .slideshow__text-wrapper {
          padding: var(--header-height, 52px) 16px 36px !important;
        }
        #block-{{ section.id }}-{{ block.id }} .heading--large {
          font-size: clamp(30px, 9.5vw, 50px) !important;
        }
        #shopify-section-{{ section.id }} #block-{{ section.id }}-{{ block.id }} .content-box.content-box--medium { max-width: 100% !important; }
      }
    {%- endif -%}

    {%- if block.settings.accent_gold -%}
      /* 燙金：照包裝上 Pro 的燙金。白底壓不住淺金，所以主體落在偏深的 #a8822a，
         只留一道窄高光；不是圖片是文字，放大不糊、搜尋引擎也讀得到字。 */
      #block-{{ section.id }}-{{ block.id }} .gc-slideshow__accent--gold {
        font-weight: 600;
        background: linear-gradient(100deg,
          #7d5e22 0%, #b8912f 11%, #e3c973 24%, #fff4d0 33%, #dcc26e 41%,
          #a8822a 55%, #8a6a24 68%, #d9b75e 84%, #9c7a2c 100%);
        background-size: 260% 100%;
        background-position: 22% 0;
        -webkit-background-clip: text; background-clip: text;
        color: transparent; -webkit-text-fill-color: transparent;
        animation: gc-gold-sweep 6.5s ease-in-out infinite;
      }
      @media (prefers-reduced-motion: reduce) {
        #block-{{ section.id }}-{{ block.id }} .gc-slideshow__accent--gold { animation: none; }
      }
    {%- endif -%}
  {%- endfor -%}
</style>

<section class="section section--flush">
  <slide-show reveal-on-scroll {% if section.settings.autoplay %}auto-play{% endif %} transition-type="{{ section.settings.transition_type | escape }}" class="slideshow slideshow--{{ section.settings.section_height }}">
    <div class="slideshow__slide-list">
      {%- for block in section.blocks -%}
        {%- comment -%}
        ------------------------------------------------------------------------------------------------------------------
        IMAGE PART
        ------------------------------------------------------------------------------------------------------------------
        {%- endcomment -%}

        {%- assign image = block.settings.image -%}
        {%- assign split_image = block.settings.split_image -%}
        {%- assign mobile_image = block.settings.mobile_image -%}

        {%- assign image_aspect_ratio = image.aspect_ratio | default: 2.63 -%}

        {%- comment -%}
        If the image is split and that we want to preserve the ratio, we have to multiply the ratio by 2 (as each image
        only account for half the screen size)
        {%- endcomment -%}

        {%- if image != blank and split_image != blank and section.settings.section_height == 'auto' -%}
          {%- assign image_aspect_ratio = image_aspect_ratio | times: 2.0 -%}
        {%- endif -%}

        {%- if forloop.index > 2 -%}
          {%- assign loading_attribute_value = 'lazy' -%}
        {%- else -%}
          {%- assign loading_attribute_value = 'eager' -%}
        {%- endif -%}

        {%- capture slide_content -%}
          <div class="slideshow__image-wrapper {% if mobile_image != blank or block.settings.asset_mobile_image != blank %}hidden-pocket{% endif %}" {% if section.settings.transition_type == 'reveal' %}reveal-visibility{% endif %}>
            {%- if block.settings.asset_image != blank -%}
              {%- comment -%}
                圖放在主題資產（assets/）而不是 Files 的情況。Files 需要 write_files 權限，
                主題資產只需要 write_themes——白底首圖是這樣上去的。

                **主題資產也吃 Shopify 的縮圖**（`name_1600x.webp` 那種網址），所以 srcset 照給。
                沒給的話所有人都下載同一份 3000px 原檔：一台 1440 的筆電會吃 229KB（2026-09-13 實測）。

                **不能用 `asset_img_url` 這個濾鏡**——它不吃 webp，會吐回 Shopify 的「無圖」佔位 gif
                （實測踩到）。所以自己把 `_<寬>x` 插進副檔名前面組網址。

                **上限要給到 3000w。**曾經停在 2400w 想省 71KB，結果 1440×2＝2880px 的螢幕
                拿 2400 去放大 1.2 倍，機身上「33/100/25W」那排小字明顯糊掉
                （量過：邊緣銳利度 57.2 vs 60.6，Mars 一眼看出來）。
                對照組：3000w 跟「完全不壓縮的原檔」只差 2%（60.6 vs 61.9）——
                **糊是放大造成的，不是 webp 壓縮造成的。省流量不要從最高那階省。**
              {%- endcomment -%}
              {%- assign gc_ws = '600,800,1000,1200,1400,1600,1800,2000,2400,3000' | split: ',' -%}
              {%- assign gc_base = block.settings.asset_image | asset_url -%}
              {%- assign gc_ext = block.settings.asset_image | split: '.' | last -%}
              {%- capture gc_dot -%}.{{ gc_ext }}{%- endcapture -%}
              {%- capture gc_srcset -%}
                {%- for w in gc_ws -%}
                  {%- capture gc_rep -%}_{{ w }}x.{{ gc_ext }}{%- endcapture -%}
                  {{- gc_base | replace: gc_dot, gc_rep }} {{ w }}w
                  {%- unless forloop.last %}, {% endunless -%}
                {%- endfor -%}
              {%- endcapture -%}
              {%- capture gc_fallback -%}_1600x.{{ gc_ext }}{%- endcapture -%}
              <img src="{{ gc_base | replace: gc_dot, gc_fallback }}"
                   srcset="{{ gc_srcset }}" sizes="100vw"
                   alt="{{ block.settings.image_alt | default: block.settings.title | escape }}"
                   loading="{{ loading_attribute_value }}"
                   {% if forloop.first %}fetchpriority="high"{% endif %}
                   draggable="false" class="slideshow__image">
            {%- elsif image != blank -%}
              {%- capture sizes_attributes -%}{% if split_image != blank %}(min-width: 1000px) 50vw{% else %}100vw{% endif %}{%- endcapture -%}
              {{- image | image_url: width: 3000 | image_tag: loading: loading_attribute_value, draggable: false, class: 'slideshow__image', sizes: sizes_attributes, widths: '600,700,800,1000,1200,1400,1600,1800,2000,2200,2400,2600,2800,3000' -}}
            {%- else -%}
              {% capture image_name %}lifestyle-{% cycle '1', '2' %}{%- endcapture -%}
              {{- image_name | placeholder_svg_tag: 'slideshow__image slideshow__image--placeholder' -}}
            {%- endif -%}
          </div>

          {%- comment -%}If a second image (split image) is uploaded, we use it. Note that we use the same ratio as the first image (to have equal column height){% endcomment %}
          {%- if image != blank and split_image != blank -%}
            <div class="slideshow__image-wrapper slideshow__image-wrapper--secondary hidden-pocket" {% if section.settings.transition_type == 'reveal' %}reveal-visibility{% endif %}>
              {{- split_image | image_url: width: 3000 | image_tag: loading: loading_attribute_value, draggable: false, class: 'slideshow__image', sizes: '50vw', widths: '600,700,800,1000,1200,1400,1600,1800,2000,2200,2400,2600,2800,3000' -}}
            </div>
          {%- endif -%}

          {%- if block.settings.asset_mobile_image != blank -%}
            <div class="slideshow__image-wrapper hidden-lap-and-up" {% if section.settings.transition_type == 'reveal' %}reveal-visibility{% endif %}>
              {%- comment -%}手機同理給到原生 1440w：430 的 iPhone ×3 ＝ 1290，1200 就得放大{%- endcomment -%}
              {%- assign gc_mws = '400,500,600,700,800,900,1000,1200,1440' | split: ',' -%}
              {%- assign gc_mbase = block.settings.asset_mobile_image | asset_url -%}
              {%- assign gc_mext = block.settings.asset_mobile_image | split: '.' | last -%}
              {%- capture gc_mdot -%}.{{ gc_mext }}{%- endcapture -%}
              {%- capture gc_msrcset -%}
                {%- for w in gc_mws -%}
                  {%- capture gc_mrep -%}_{{ w }}x.{{ gc_mext }}{%- endcapture -%}
                  {{- gc_mbase | replace: gc_mdot, gc_mrep }} {{ w }}w
                  {%- unless forloop.last %}, {% endunless -%}
                {%- endfor -%}
              {%- endcapture -%}
              {%- capture gc_mfallback -%}_800x.{{ gc_mext }}{%- endcapture -%}
              <img src="{{ gc_mbase | replace: gc_mdot, gc_mfallback }}"
                   srcset="{{ gc_msrcset }}" sizes="100vw"
                   alt="{{ block.settings.image_alt | default: block.settings.title | escape }}"
                   loading="{{ loading_attribute_value }}"
                   {% if forloop.first %}fetchpriority="high"{% endif %}
                   draggable="false" class="slideshow__image">
            </div>
          {%- elsif mobile_image != blank -%}
            <div class="slideshow__image-wrapper hidden-lap-and-up" {% if section.settings.transition_type == 'reveal' %}reveal-visibility{% endif %}>
              {{- mobile_image | image_url: width: 3000 | image_tag: loading: loading_attribute_value, draggable: false, class: 'slideshow__image', sizes: '100vw', widths: '600,700,800,1000,1200,1400,1600,1800,2000,2200,2400,2600,2800,3000' -}}
            </div>
          {%- endif -%}

          {%- comment -%}
          ----------------------------------------------------------------------------------------------------------------
          CONTENT PART
          ----------------------------------------------------------------------------------------------------------------
          {%- endcomment -%}

          {%- capture section_content -%}
            {%- if block.settings.subheading != blank -%}
              <h2 class="heading heading--small">
                <split-lines {% if section.settings.transition_type != 'fade' %}reveal{% endif %}>{{ block.settings.subheading | escape }}</split-lines>
              </h2>
            {%- endif -%}

            {%- if block.settings.title != blank -%}
              <h3 class="heading heading--large">
                {%- if block.settings.title_accent != blank -%}
                  {{ block.settings.title | escape }} <em class="gc-slideshow__accent{% if block.settings.accent_gold %} gc-slideshow__accent--gold{% endif %}">{{ block.settings.title_accent | escape }}</em>
                {%- else -%}
                  <split-lines {% if section.settings.transition_type != 'fade' %}reveal{% endif %}>{{ block.settings.title | escape }}</split-lines>
                {%- endif -%}
              </h3>
            {%- endif -%}

            {%- if block.settings.text != blank -%}
              <p class="gc-slideshow__tagline">{{ block.settings.text | escape }}</p>
            {%- endif -%}

            {%- if block.settings.text_2 != blank -%}
              <p class="gc-slideshow__tagline gc-slideshow__tagline--sub">{{ block.settings.text_2 | escape }}</p>
            {%- endif -%}

            {%- capture buttons -%}
              {%- if block.settings.button_1_text != blank -%}
                <a href="{{ block.settings.button_1_link }}" class="button button--primary">{{ block.settings.button_1_text | escape }}</a>
              {%- endif -%}

              {%- if block.settings.button_2_text != blank -%}
                <a href="{{ block.settings.button_2_link }}" class="button button--primary">{{ block.settings.button_2_text | escape }}</a>
              {%- endif -%}
            {%- endcapture -%}

            {%- if block.settings.button_1_text != blank and block.settings.button_2_text != blank -%}
              <div class="button-group" {% if section.settings.transition_type != 'fade' %}reveal{% endif %}>
                <div class="button-group__wrapper">
                  {{- buttons -}}
                </div>
              </div>
            {%- elsif buttons != blank -%}
              <div class="button-wrapper" {% if section.settings.transition_type != 'fade' %}reveal{% endif %}>
                {{- buttons -}}
              </div>
            {%- endif -%}
          {%- endcapture -%}

          {%- if section_content != blank -%}
            <div class="container">
              {%- assign text_alignment = block.settings.text_position | split: '_' | first -%}
              {%- assign text_position = block.settings.text_position | split: '_' | last -%}

              <div class="slideshow__text-wrapper slideshow__text-wrapper--{{ text_alignment }} vertical-breather">
                <div class="content-box content-box--medium content-box--text-{{ text_position }} content-box--{{ text_position }} text-container">
                  {{- section_content -}}
                </div>
              </div>
            </div>
          {%- endif -%}
        {%- endcapture -%}

        {%- comment -%}If only the button 1 link is filled, then we make the whole slide clickable{%- endcomment -%}

        {%- comment -%}
          這張圖是深底還是淺底，直接沿用它本來就有的「文字顏色」欄位——
          文字設白＝底圖深、文字設深＝底圖淺。不另外開一個欄位，
          也不做自動判斷（Mars 拍板：每張 banner 手動標，判斷錯比沒有更糟）。
          light ＝ 頁首要翻白；dark ＝ 頁首維持原本的紅 logo 與深色圖示。
        {%- endcomment -%}
        {%- assign txt_bright = block.settings.text_color | color_brightness -%}
        {%- if txt_bright > 128 -%}
          {%- assign gc_ink = 'light' -%}
        {%- else -%}
          {%- assign gc_ink = 'dark' -%}
        {%- endif -%}

        {%- capture slide_attributes -%}
          {{ block.shopify_attributes }}
          data-gc-ink="{{ gc_ink }}"
          id="block-{{ section.id }}-{{ block.id }}"
          class="slideshow__slide {% if split_image != blank %}slideshow__slide--split{% endif %} {% if section.settings.transition_type == 'sweep' %}slideshow__slide--sweep{% endif %}"
          {% unless forloop.first %}hidden{% endunless %}
          {% if section.settings.section_height == 'auto' %}
            style="--image-aspect-ratio: {{ image_aspect_ratio }}; --mobile-image-aspect-ratio: {{ mobile_image.aspect_ratio | default: image_aspect_ratio | default: 1 }};"
          {% endif %}
        {%- endcapture -%}

        <slide-show-item {% if section.settings.transition_type != 'reveal' %}reveal-visibility{% endif %} {{ slide_attributes }}>
          {%- if block.settings.button_1_link != blank and block.settings.button_1_text == blank and block.settings.button_2_text == blank -%}
            <a class="slideshow__slide-inner" href="{{ block.settings.button_1_link }}">
              {{- slide_content -}}
            </a>
          {%- else -%}
            <div class="slideshow__slide-inner">
              {{- slide_content -}}
            </div>
          {%- endif -%}
        </slide-show-item>
      {%- endfor -%}
    </div>

    {%- if section.blocks.size > 1 -%}
      <page-dots {% if section.settings.autoplay %}animation-timer{% endif %} class="slideshow__nav container">
        {%- for block in section.blocks -%}
          <button class="slideshow__progress-bar" aria-controls="block-{{ section.id }}-{{ block.id }}" {% if forloop.first %}aria-current="true"{% endif%}>
            <span class="visually-hidden">{{ 'general.accessibility.go_to_slide' | t: num: forloop.index }}</span>
          </button>
        {%- endfor -%}
      </page-dots>
    {%- endif -%}
  </slide-show>
</section>

{%- comment -%}
  頁首要不要透明、要翻白還是維持深色，由「現在顯示的是哪一張」決定。
  結果寫在 <html data-gc-hero="light|dark"> 上，實際的樣式在 gc-header.css 第 19 段。

  為什麼掛在 html 而不是直接改頁首：頁首是主題的區塊，主題自己會在很多時候
  重寫它的 class（大選單開關那次就是這樣一直閃）。把狀態放在 html 上，
  主題碰不到，我們的 CSS 也只是讀它。

  何時算「頁首壓在首圖上」：首圖的底還在頁首下緣以下。捲過首圖就移除，
  頁首交還給 gc-header.js 的 .gc-solid（白色霧面玻璃）。
{%- endcomment -%}
<script>
  (function () {
    var root = document.getElementById('shopify-section-{{ section.id }}');
    if (!root) return;
    var html = document.documentElement;
    var raf = 0;
    var SECTION_ID = '{{ section.id }}';

    /* 只清掉自己設的。首頁上白底首圖是另外一段，它也會設這個屬性。 */
    function release() {
      if (html.getAttribute('data-gc-hero-by') === SECTION_ID) {
        html.removeAttribute('data-gc-hero');
        html.removeAttribute('data-gc-hero-by');
      }
    }

    function activeInk() {
      var s = root.querySelector('slide-show-item:not([hidden])');
      return (s && s.getAttribute('data-gc-ink')) || 'light';
    }
    function headerH() {
      var h = document.querySelector('.header');
      return h ? h.getBoundingClientRect().height : 52;
    }
    function sync() {
      raf = 0;
      var r = root.getBoundingClientRect();
      if (r.bottom > headerH() && r.top <= headerH()) {
        html.setAttribute('data-gc-hero', activeInk());
        html.setAttribute('data-gc-hero-by', SECTION_ID);
      } else { release(); }
    }
    function tick() { if (!raf) raf = requestAnimationFrame(sync); }

    /* 換頁時 Turbo 會把 html 上的屬性留著，離開首頁一定要清掉 */
    addEventListener('turbo:before-render', release);
    addEventListener('scroll', tick, { passive: true });
    addEventListener('resize', tick);
    /* 換到下一張時 Focal 是切 hidden 屬性，用它當訊號 */
    new MutationObserver(tick).observe(root, { subtree: true, attributes: true, attributeFilter: ['hidden'] });
    sync();
  })();
</script>

{% schema %}
{
  "name": "GC 輪播",
  "class": "shopify-section--slideshow",
  "max_blocks": 5,
  "settings": [
    {
      "type": "header",
      "content": "白底首圖（gc 自訂）"
    },
    {
      "type": "checkbox",
      "id": "tuck_under_header",
      "label": "塞到頁首底下（只有它是頁面第一段時才開）",
      "default": true,
      "info": "上面還有別的區段時要關掉，不然這一段會往上蓋掉前一段的下緣。"
    },
    {
      "type": "number",
      "id": "custom_height_desktop_vw",
      "label": "桌機圖片自然高度（vw＝圖高÷圖寬×100）",
      "info": "輪播的高度是整段共用的，改這個會同時改變裡面每一張的高度（其餘幾張是裁切填滿，會被多裁一些）。"
    },
    {
      "type": "number",
      "id": "custom_height_mobile_vw",
      "label": "手機圖片自然高度（vw＝圖高÷圖寬×100）"
    },
    {
      "type": "number",
      "id": "max_height_px",
      "label": "整段高度上限（px，0 = 不限制）",
      "info": "超寬螢幕用。到頂之後圖片改以高度為準等比縮，左右露出的是底色。"
    },
    {
      "type": "select",
      "id": "section_height",
      "label": "Section height",
      "options": [
        {
          "value": "auto",
          "label": "Original image ratio"
        },
        {
          "value": "small",
          "label": "Small"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "large",
          "label": "Large"
        },
        {
          "value": "fit",
          "label": "Fit screen height"
        }
      ],
      "info": "Choose \"Original image ratio\" to not cut images. [Learn more](https://help.shopify.com/en/manual/online-store/images/theme-images#best-practices-for-slideshows-and-full-width-images)",
      "default": "auto"
    },
    {
      "type": "select",
      "id": "transition_type",
      "label": "Transition type",
      "options": [
        {
          "value": "sweep",
          "label": "Sweep"
        },
        {
          "value": "reveal",
          "label": "Reveal"
        },
        {
          "value": "fade",
          "label": "Fade"
        }
      ],
      "default": "sweep"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Auto rotate between slides",
      "default": true
    },
    {
      "type": "range",
      "id": "cycle_speed",
      "min": 4,
      "max": 20,
      "step": 1,
      "unit": "sec",
      "label": "Change slides every",
      "default": 5
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "info": "Used while slideshow image is loading",
      "default": "rgba(0,0,0,0)"
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "info": "2160 x 1080px .jpg recommended, 1080 x 1080px .jpg recommended if split",
          "label": "Image"
        },
        {
          "type": "image_picker",
          "id": "split_image",
          "info": "1080 x 1080px .jpg recommended. Won't show up on mobile.",
          "label": "Split image"
        },
        {
          "type": "image_picker",
          "id": "mobile_image",
          "label": "Mobile image",
          "info": "1000 x 1400px .jpg recommended. If none is set, desktop image will be used."
        },
        {
          "type": "header",
          "content": "Content"
        },
        {
          "type": "select",
          "id": "text_position",
          "label": "Text position",
          "options": [
            {
              "value": "top_left",
              "label": "Top left"
            },
            {
              "value": "top_center",
              "label": "Top center"
            },
            {
              "value": "top_right",
              "label": "Top right"
            },
            {
              "value": "middle_left",
              "label": "Middle left"
            },
            {
              "value": "middle_center",
              "label": "Middle center"
            },
            {
              "value": "middle_right",
              "label": "Middle right"
            },
            {
              "value": "bottom_left",
              "label": "Bottom left"
            },
            {
              "value": "bottom_center",
              "label": "Bottom center"
            },
            {
              "value": "bottom_right",
              "label": "Bottom right"
            }
          ],
          "default": "middle_center"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "Subheading",
          "default": "Tell your story"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Heading",
          "default": "Slide title"
        },
        {
          "type": "text",
          "id": "text",
          "label": "說明文字（大標下方）"
        },
        {
          "type": "text",
          "id": "text_2",
          "label": "說明文字第二行（更小、更淡）"
        },
        {
          "type": "checkbox",
          "id": "accent_gold",
          "label": "大標尾字改燙金（取代藍漸層）",
          "default": false
        },
        {
          "type": "header",
          "content": "白底版型（gc 自訂）"
        },
        {
          "type": "checkbox",
          "id": "light_layout",
          "label": "白底版型：字在上、產品貼底",
          "default": false,
          "info": "給白底圖用。字不疊在圖上，改放在圖上方的白區裡。"
        },
        {
          "type": "color",
          "id": "light_bg",
          "label": "白底版型的底色",
          "default": "#fefefe",
          "info": "要跟圖片最上緣的白同色，差幾個色階交界就會看得出一條邊。"
        },
        {
          "type": "number",
          "id": "light_white_pct",
          "label": "桌機：文字區佔整段高度的 %"
        },
        {
          "type": "number",
          "id": "light_white_pct_mobile",
          "label": "手機：文字區佔整段高度的 %"
        },
        {
          "type": "header",
          "content": "圖片改用主題資產（gc 自訂）"
        },
        {
          "type": "text",
          "id": "asset_image",
          "label": "桌機圖檔名（assets/ 底下）",
          "info": "填了就蓋過上面的圖片選擇器。給「圖片放在主題資產、不在 Files」的情況用。"
        },
        {
          "type": "text",
          "id": "asset_mobile_image",
          "label": "手機圖檔名（assets/ 底下）"
        },
        {
          "type": "text",
          "id": "image_alt",
          "label": "圖片替代文字（主題資產圖才需要）"
        },
        {
          "type": "text",
          "id": "title_accent",
          "label": "大標尾字（細字藍漸層，例：Air）"
        },
        {
          "type": "text",
          "id": "button_1_text",
          "label": "Button 1 text"
        },
        {
          "type": "url",
          "id": "button_1_link",
          "label": "Button 1 link",
          "info": "Leave the \"Button 1 text\" and \"Button 2\" settings empty to make the slide fully clickable."
        },
        {
          "type": "text",
          "id": "button_2_text",
          "label": "Button 2 text"
        },
        {
          "type": "url",
          "id": "button_2_link",
          "label": "Button 2 link"
        },
        {
          "type": "header",
          "content": "Colors"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text",
          "default": "#ffffff"
        },
        {
          "type": "color",
          "id": "button_background",
          "label": "Button background",
          "default": "#ffffff"
        },
        {
          "type": "color",
          "id": "button_text_color",
          "label": "Button text",
          "default": "#000000"
        },
        {
          "type": "color",
          "id": "overlay_color",
          "label": "Overlay",
          "default": "#000000"
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "label": "Overlay opacity",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 30
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "GC 輪播",
      "blocks": [
        {
          "type": "image",
          "settings": {
            "title": "Slide 1"
          }
        },
        {
          "type": "image",
          "settings": {
            "title": "Slide 2"
          }
        }
      ]
    }
  ]
}
{% endschema %}