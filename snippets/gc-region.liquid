{%- comment -%}
  gc 地區選擇器（2026-08-29）

  位置：頁尾底欄右側，接在版權行後面（Mars 拍板位置 A）。
  實測 Apple 台灣與 Anker 美國都放在頁尾最底（全頁 99%），
  而我們的法律底欄剛好就在那個位置，所以是零新增結構。
  DJI 放頁首也成立，但那裡要跟搜尋、購物車搶注意力，而換地區是一輩子一次的動作。

  排版：分組式（Mars 從四個版本裡選的）。
  用一條細線把「目前開放」與「即將推出」分開——不是三個選項裡有兩個是灰的，
  而是兩組不同狀態的清單。等美國站開了，那條線會自己往下移。

  沒有用 JavaScript：<details> 是瀏覽器原生的展開元件，鍵盤與螢幕閱讀器都支援，
  JS 掛掉也還能用。代價是點外面不會自動收起來——對頁尾的元件可以接受。

  ── 要新增／開通地區，只改下面這一行 ──
  格式：代碼|地區名|語言|網域|是否已上線(1/0)，用逗號分隔。
  站開了就把該地區最後的 0 改成 1，它會自動從「即將推出」移到「目前開放」。

  網域策略：國別網域（Mars 2026-08-29 拍板）。
  馬來西亞的 grantclassic.my 是照同一規則推的，尚未確認。

  要還原：把 gc-footer-legal.liquid 裡 render 這支的那一行拿掉，再刪這個檔案。
{%- endcomment -%}

{%- liquid
  assign region_data = 'TW|台灣|繁體中文|grantclassic.com|1,US|美國|English|grantclassic.us|1,MY|馬來西亞|English|grantclassic.my|0'
  assign current_code = 'TW'
  assign rows = region_data | split: ','
-%}

{%- comment -%}先算出目前地區的名字，按鈕上要顯示{%- endcomment -%}
{%- assign current_name = '' -%}
{%- for row in rows -%}
  {%- assign f = row | split: '|' -%}
  {%- if f[0] == current_code -%}{%- assign current_name = f[1] -%}{%- endif -%}
{%- endfor -%}

<details class="gc-region">
  <summary class="gc-region__toggle">
    <svg class="gc-region__globe" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true">
      <circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3c2.6 2.6 2.6 15.4 0 18M12 3c-2.6 2.6-2.6 15.4 0 18"/>
    </svg>
    <span class="gc-region__now">{{ current_name }}</span>
    <svg class="gc-region__chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
      <path d="M6 9l6 6 6-6"/>
    </svg>
  </summary>

  <div class="gc-region__panel" role="group" aria-label="選擇地區">
    <p class="gc-region__grp">目前開放</p>
    {%- for row in rows -%}
      {%- assign f = row | split: '|' -%}
      {%- if f[4] == '1' -%}
        {%- if f[0] == current_code -%}
          <span class="gc-region__opt is-current" aria-current="true">
            <svg class="gc-region__tick" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" aria-hidden="true"><path d="M4 12.5l5.5 5.5L20 7"/></svg>
            <span class="gc-region__name">{{ f[1] }}</span>
            <span class="gc-region__lang">{{ f[2] }}</span>
          </span>
        {%- else -%}
          <a class="gc-region__opt" href="https://{{ f[3] }}/" target="_blank" rel="noopener">
            <span class="gc-region__tick" aria-hidden="true"></span>
            <span class="gc-region__name">{{ f[1] }}</span>
            <span class="gc-region__lang">{{ f[2] }}</span>
          </a>
        {%- endif -%}
      {%- endif -%}
    {%- endfor -%}

    {%- assign has_soon = false -%}
    {%- for row in rows -%}
      {%- assign f = row | split: '|' -%}
      {%- if f[4] != '1' -%}{%- assign has_soon = true -%}{%- endif -%}
    {%- endfor -%}

    {%- if has_soon -%}
      <div class="gc-region__div"></div>
      <p class="gc-region__grp">即將推出</p>
      {%- for row in rows -%}
        {%- assign f = row | split: '|' -%}
        {%- if f[4] != '1' -%}
          <span class="gc-region__opt is-soon">
            <span class="gc-region__tick" aria-hidden="true"></span>
            <span class="gc-region__name">{{ f[1] }}</span>
            <span class="gc-region__lang">{{ f[2] }}</span>
          </span>
        {%- endif -%}
      {%- endfor -%}
    {%- endif -%}
  </div>
</details>
