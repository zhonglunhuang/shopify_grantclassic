{%- comment -%}
  購買卡（2026-08-19 Mars 拍板放在比較表之後、常見問題之前，ts-compare 內 render）。
  2026-09-15（GOS-0286）：內容換成 Apple 式選購流程段 gc-shop（顏色 → 買幾件 → 配件，摺疊步驟＋底部摘要列），
  套餐／入組／加購讀 GrantOS 寫的商品欄位；頂部列與浮動列的「購買」照樣跳 #buy（gc-shop 的區段 id 就是 buy）。
  舊版卡片在 git 歷史（本檔 2026-09-15 之前）。
{%- endcomment -%}
{%- render 'gc-shop', product: product, title: 'TITANSHIELD Air', stage_map: '黑=ts-buy-black.png|銀=ts-buy-silver.png' -%}
