{%- comment -%}
  圖片佔位框。用法：{% render 'df-ph', label: '車頭實裝照', hint: '橫幅、白天、乾淨背景' %}

  Mars 要求「先不要擺圖，先把視覺定好」，所以沒指定圖片的位置顯示這個，
  上面寫著該拍什麼——版面看得出比例，同時也是拍攝清單。
{%- endcomment -%}
<div class="df__ph{{ extra }}" style="{{ ratio }}">
  <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
    <rect x="3" y="5" width="18" height="14" rx="2.5" stroke="currentColor" stroke-width="1.5"/>
    <circle cx="8.5" cy="10" r="1.5" fill="currentColor"/>
    <path d="M4 17l4.5-4.5 3 3L15 12l5 5" stroke="currentColor" stroke-width="1.5"
          stroke-linecap="round" stroke-linejoin="round"/>
  </svg>
  {%- comment -%}
    ！`default` 把空字串也當成「沒給」，所以想「完全不要標題」時
      不能傳空字串（會變成預設的「這裡放圖」）——要傳 no_label: true。
      名稱已經印在框外面的地方會用到（例如隨附配件那一格）。
  {%- endcomment -%}
  {%- unless no_label -%}
    <b>{{ label | default: '這裡放圖' }}</b>
  {%- endunless -%}
  {%- if hint != blank -%}<span>{{ hint }}</span>{%- endif -%}
</div>
