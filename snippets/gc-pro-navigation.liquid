{% doc %}
  Adapts the Pro product bar to the shared two-level product navigation.
{% enddoc %}
<style>
  .gc-air-navigation .tsp-pro .topbar.ts-topbar {
    height: 48px;
    z-index: 40;
    background: #1d1d1f !important;
    color: #f5f5f7;
    border: 0;
    box-shadow: none;
  }
  .gc-air-navigation .tsp-pro .topbar .ts-topbar__in {
    max-width: none;
    height: 100%;
    padding-left: var(--gc-air-content-left, 32px);
    padding-right: var(--gc-air-content-right, 32px);
  }
  .gc-air-navigation .tsp-pro .topbar .ts-topbar__name {
    color: #f5f5f7;
    font-size: 14px;
  }
  .gc-air-navigation .tsp-pro .topbar .spy a { color: #c7c7cc; }
  .gc-air-navigation .tsp-pro .topbar .spy a:hover,
  .gc-air-navigation .tsp-pro .topbar .spy a.on { color: #fff; }
  .gc-air-navigation .tsp-pro .topbar .ts-topbar__buy {
    height: 28px;
    min-width: 48px;
    padding: 0 12px;
    border-radius: 5px;
    font-size: 12px;
    line-height: 1;
  }
  .gc-air-navigation .tsp-pro .ts-topbar__spacer {
    height: var(--gc-air-product-height);
    background: #1d1d1f;
  }
  @media (max-width: 560px) {
    .gc-air-navigation .tsp-pro .topbar.ts-topbar { height: 44px; }
  }
</style>
